# HANDOFF — 18/09/2026 (retour prévu mardi ~1h)

> Écrit pour toi, à lire au réveil. Court, honnête, priorisé. La todo ordonnée
> vit dans `docs/A-FAIRE-ZAKARIA.md` (mise à jour aujourd'hui) ; ici c'est ce
> que je PENSE, pas seulement ce qu'il reste à cocher.

## En une phrase
L'infra de prod est **déployée et l'auth est prouvée**, l'envoi email est
**câblé mais pas encore prouvé** (un test à faire). **Zéro vente, toujours** —
et c'est ça le vrai sujet, pas l'infra.

## Ce qui tourne EN PROD maintenant (vérifié, pas supposé)
- **Netlify**, site `alphasalesos`, deploy **Published**. (Vercel abandonné : je
  ne peux pas le piloter depuis le sandbox, Netlify oui.)
- **Connexion prouvée** : `REQUIRE_AUTH=1` + login réussi avec
  `contact@eagleyecorp.fr`. Première fois que la bascule auth est prouvée en vrai.
- **11 variables** en place (comptes, Supabase, JWT, SMTP, fix scan). Aucune
  valeur secrète n'est passée par le chat.
- Code vert (`tsc` + tests), build de prod reproduit en local (78/78 pages).

## Le SEUL point non prouvé (5 min, toi)
- **`/recette`** → adresse test `eagleyecorp.ad@gmail.com` → envoyer. `/api/send`
  se dit « configured » mais ça ne prouve PAS le mot de passe SMTP ; seul un
  **email qui arrive** le prouve. Erreur `EAUTH`/`535` = mot de passe faux ;
  timeout = port 465.

## Ce qui bloque les VENTES, dans l'ordre
1. **Prouver l'envoi** (ci-dessus).
2. **Délivrabilité DNS Amen** — SPF (un seul `v=spf1` incluant `securemail.pro`),
   DKIM (activé le 16/09, sélecteur `s=` à relever), DMARC. Sans ça la
   prospection tombe en spam **et la même boîte porte devis + inscriptions
   Supabase**. La vérif DNS n'est PAS bloquée depuis le sandbox → une prochaine
   session peut la diagnostiquer sans toi.
3. **Domaine** `alphasalesos.eagleyecorp.fr` → Netlify, puis `APP_BASE_URL`.
4. **PROSPECTER POUR DE VRAI.** L'outil sait cibler (maître d'ouvrage à permis
   actif, Lyon/Villeurbanne), écrire, cadencer. Il n'a **jamais servi à un vrai
   email ni un vrai appel.** Canal par défaut : LinkedIn (les permis ne portent
   pas de numéro). Les 8 fiches ICP existent ; le collecteur de permis
   (`scripts/permis-lyon.mjs`) est là mais les sources open-data françaises sont
   bloquées depuis le sandbox → l'export se fait de ton côté.

## Vérité brutale (tu me l'as demandé)
- **0 vente, 0 permis converti.** Toute la tarification (socle 600, siège 80,
  packs, part au résultat, grille Alpha Voice) est un empilement de **décisions,
  pas de mesures**. Le premier client qui refuse **en disant pourquoi** vaut plus
  que tout ce raffinage.
- **L'infra a mangé la session.** C'était nécessaire — sans envoi qui marche, il
  n'y a pas de prospection — mais poser des variables ne fait entrer aucun euro.
- **Le vrai risque, c'est de continuer à polir l'outil au lieu de l'utiliser.**
  Après l'envoi prouvé + le DNS, la seule chose qui compte est d'envoyer de
  VRAIS messages à de VRAIS maîtres d'ouvrage et d'écouter ce qui revient. Le
  produit est prêt à ~90 % pour ça ; les 10 % restants n'apprennent rien qu'un
  premier « non » motivé n'apprendrait mieux.

## Pièges à ne PAS re-déclencher (pour la prochaine session)
- **Secrets** : jamais collés en chat. Le `SUPABASE_JWT_SECRET` est le vrai
  HS256 (login prouvé) — pas un UUID. Ne pas y retoucher.
- **Scan de secrets Netlify** : désarmé seulement sur 4 clés non-secrètes
  (`SECRETS_SCAN_OMIT_KEYS`). **Jamais** `SECRETS_SCAN_ENABLED=false`.
- **Ordre** : SMTP posé APRÈS comptes + `REQUIRE_AUTH` (respecté). Ne pas
  inverser.
- **Sandbox** : `*.netlify.app` et Supabase sont **bloqués** (403) → tout test
  live se fait de ton côté. Le sandbox PEUT : github, **DNS public**, build
  local, Chromium (rendu). C'est pour ça que le DNS est délégable à une session.

## Backlog plus loin (pas bloquant pour lancer)
- Ordonnanceur `pg_cron` (migrations 004/005) + secret dans le Vault Supabase.
- `SITE_PASSWORD` pour les préfixes admin (`/payouts`, `/offre`, `/api/sync`).
- Fait cette session : routage offre/verticale corrigé, page anglaise, brand
  kit, pont Maps→Alpha, doublon mort de devis supprimé.

## Si je devais faire UNE chose à ta place demain
Prouver l'envoi sur `/recette`, puis m'écrire « oui DNS » — je diagnostique
SPF/DKIM/DMARC pendant que tu dors, pour qu'au réveil il ne reste qu'à coller
trois enregistrements chez Amen. Après ça, on prospecte.
